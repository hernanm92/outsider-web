<link rel="stylesheet" href="assets/css/pages/page_search_inner_tables.css">

<div class="container content-sm">

    <div class="text-center margin-bottom-35">
        <a href="#admin" class="btn btn-default"><i class="glyphicon glyphicon-backward"></i> Volver</a>
        <a href="#admin/photo" class="btn btn-info"><i class="glyphicon glyphicon-plus-sign"></i> Nueva foto</a>
    </div>
    <!-- Begin Table Search v2 -->
    <div class="table-search-v2">
        <div class="table-responsive">
            <table class="table table-bordered table-striped">
                <thead>
                <tr>
                    <th>Foto</th>
                    <th class="hidden-sm">Nombre</th>
                    <th>Deporte</th>
                    <th>Lugar</th>
                    <th>Acciones</th>
                </tr>
                </thead>
                <tbody>
                <tr ng-repeat="photo in photos">
                    <td>
                        <img class="img-bordered" src="{{photo.url}}" alt="">
                    </td>
                    <td class="td-width">
                        <h3><a href="#">{{photo.title}}</a></h3>
                        <p>{{photo.description}}</p>
                    </td>
                    <td>
                        <span class="label label-success" >{{photo.sport}}</span>
                    </td>
                    <td>
                        <span><a href="#">{{photo.spot.name}}</a></span>
                    </td>
                    <td>
                        <ul class="list-inline table-buttons">
                            <li><button type="button" class="btn-u btn-u-sm btn-u-dark" ng-click="editPhoto(photo.id)"><i class="fa fa-edit"></i></button></li>
                            <li><button type="button" class="btn-u btn-u-sm btn-u-dark" ng-click="deletePhoto(photo.id)"><i class="fa fa-trash-o"></i></button></li>
                        </ul>
                    </td>
                </tr>
                </tbody>
            </table>
        </div>
    </div>
    <!-- End Table Search v2 -->
</div>